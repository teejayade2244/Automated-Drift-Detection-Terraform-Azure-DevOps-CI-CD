trigger: none

schedules:
- cron: "0 9 * * *"
  displayName: Daily Terraform Drift Detection
  branches:
    include:
    - master
  always: true

variables:
  - group: SlackSecrets  
  - group: AzureCredentials  # Add this variable group with Azure credentials
  - name: terraformVersion
    value: '1.5.0'
  - name: workingDirectory
    value: '$(System.DefaultWorkingDirectory)'
  - name: azureServiceConnection
    value: 'Azure-Resource-Manager' 
  - name: tfBackendResourceGroup
    value: 'Temitope'
  - name: tfBackendStorageAccount
    value: 'sttfstateorageacc'
  - name: tfBackendContainer
    value: 'tfstate'
  - name: tfBackendKey
    value: 'drift-detection.tfstate'

# Self-hosted agent pool
pool:
  name: 'Pipeline Agent'
  demands:
  - agent.os -equals Linux

stages:

# --------------------------------------------------
# TERRAFORM DRIFT DETECTION
# --------------------------------------------------
- stage: DriftDetection
  displayName: Terraform Drift Detection

  jobs:
  - job: DetectDrift
    displayName: Install, Init, Validate & Plan
    pool:
      name: 'Pipeline Agent'

    steps:
    - checkout: self
      displayName: 'Checkout Repository'

    # Install Azure CLI on self-hosted agent
    - task: Bash@3
      displayName: 'Install Azure CLI'
      inputs:
        targetType: 'inline'
        script: |
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
          az --version

    # Verify Terraform is accessible
    - task: Bash@3
      displayName: 'Verify Terraform Installation'
      inputs:
        targetType: 'inline'
        script: |
          echo "Checking Terraform installation..."
          which terraform
          terraform version
          echo "Terraform is properly installed"

    # Debug: Check if variables are set
    - task: Bash@3
      displayName: 'Debug: Check Variables'
      inputs:
        targetType: 'inline'
        script: |
          echo "Checking if Azure variables are set..."
          echo "ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)"
          echo "ARM_TENANT_ID: $(ARM_TENANT_ID)"
          echo "ARM_CLIENT_ID: $(ARM_CLIENT_ID)"
          # Don't print the secret for security

    # Make drift_detection.sh executable
    - task: Bash@3
      displayName: 'Make Script Executable'
      inputs:
        targetType: 'inline'
        script: |
          chmod +x $(workingDirectory)/drift_detection.sh
          ls -la $(workingDirectory)/drift_detection.sh

    # Terraform Init using Azure CLI authentication (simpler approach)
    - task: Bash@3
      displayName: 'Terraform Init'
      inputs:
        targetType: 'inline'
        script: |
          cd $(workingDirectory)
          echo "Initializing Terraform backend..."
          
          # Use Azure CLI for authentication instead of service principal
          az login --identity  # Use managed identity if available
          
          terraform init \
            -backend-config="storage_account_name=$(tfBackendStorageAccount)" \
            -backend-config="container_name=$(tfBackendContainer)" \
            -backend-config="key=$(tfBackendKey)" \
            -backend-config="resource_group_name=$(tfBackendResourceGroup)"
          
          echo "Terraform init completed with exit code: $?"

    # Terraform Validate
    - task: Bash@3
      displayName: 'Terraform Validate'
      inputs:
        targetType: 'inline'
        script: |
          cd $(workingDirectory)
          echo "Validating Terraform configuration..."
          terraform validate
          VALIDATE_EXIT=$?
          echo "Validation completed with exit code: $VALIDATE_EXIT"
          exit $VALIDATE_EXIT

    # Terraform Plan via script
    - task: Bash@3
      name: AnalyzeExitCode
      displayName: 'Terraform Plan & Analyze Exit Code'
      inputs:
        targetType: 'filePath'
        filePath: '$(workingDirectory)/drift_detection.sh'
        workingDirectory: '$(workingDirectory)'

    # Publish tfplan
    - task: PublishBuildArtifacts@1
      displayName: 'Publish Terraform Plan Artifact'
      condition: succeededOrFailed()
      inputs:
        PathtoPublish: '$(workingDirectory)/tfplan'
        ArtifactName: 'terraform-drift-plan-$(Build.BuildId)'
        publishLocation: 'Container'

    # Publish drift report if drift detected
    - task: PublishBuildArtifacts@1
      displayName: 'Publish Drift Report'
      condition: and(succeededOrFailed(), eq(variables['AnalyzeExitCode.planExitCode'], '2'))
      inputs:
        PathtoPublish: '$(workingDirectory)/drift-report.txt'
        ArtifactName: 'drift-report-$(Build.BuildId)'
        publishLocation: 'Container'


# --------------------------------------------------
# SLACK NOTIFICATION STAGE
# --------------------------------------------------
- stage: Notify
  displayName: "💬 Send Notifications"
  dependsOn: DriftDetection
  condition: always()

  jobs:
  - job: SendSlackNotification
    displayName: Send Slack Alert
    pool:
      name: 'Pipeline Agent'

    variables:
      planExitCode: $[ stageDependencies.DriftDetection.DetectDrift.outputs['AnalyzeExitCode.planExitCode'] ]

    steps:
    - task: Bash@3
      displayName: 'Send Slack Drift Notification'
      inputs:
        targetType: inline
        script: |
          EXIT_CODE="$(planExitCode)"

          PIPELINE_URL="$(System.CollectionUri)$(System.TeamProject)/_build/results?buildId=$(Build.BuildId)"
          NOW=$(date -u +"%Y-%m-%d %H:%M:%S UTC")

          case "$EXIT_CODE" in
            0)
              COLOR="good"
              MESSAGE="✅ *No Drift Detected*\nEverything is up to date."
              ;;
            2)
              COLOR="warning"
              MESSAGE="⚠️ *Terraform Drift Detected!*\nReview the Terraform plan."
              ;;
            1)
              COLOR="danger"
              MESSAGE="❌ *Terraform Error*\nPlan failed. Check logs."
              ;;
            *)
              COLOR="#808080"
              MESSAGE="❓ *Unknown Exit Code:* \`$EXIT_CODE\`"
              ;;
          esac

          MESSAGE="$MESSAGE\n\n• Build: \`$(Build.BuildNumber)\`\n• Branch: \`$(Build.SourceBranchName)\`\n• Time: \`$NOW\`\n• <$PIPELINE_URL|View Pipeline>"

          curl -X POST -H 'Content-type: application/json' \
            --data "{
              \"attachments\": [{
                \"color\": \"$COLOR\",
                \"text\": \"$MESSAGE\",
                \"footer\": \"Azure DevOps - Terraform Drift Detection\",
                \"ts\": $(date +%s)
              }]
            }" \
            "$(SLACK_WEBHOOK_URL)"

          echo "Slack notification sent."